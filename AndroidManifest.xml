<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="sas.bd"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">

								<!-- main menu activity -->
        <activity android:name=".BrewDroidMainMenu" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AttenuationCalculator" android:label="@string/attenuation_calculator"/>
        <activity android:name=".HydrometerCalculator" android:label="@string/hydrometer_calculator"/>
        <activity android:name=".GrainList" android:label="@string/grain_list"/>
        <activity android:name=".EditGrain" android:label="@string/edit_grain"/>

								<!-- recipe related -->
        <activity android:name=".RecipeTabs" android:label="@string/recipes"/>
        <activity android:name=".RecipeStyle" android:label="@string/style"/>

    </application>
</manifest> 
